<?xml version="1.0" encoding="utf-8"?>

<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"

    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.fragments.RoomFilterFragment">

    <!-- TEXT VIEW -->

    <TextView
        android:id="@+id/fragment_filter_room_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_normal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="@string/select_a_room_for_the_filter"
        style="@style/Title" />

    <!-- SPINNER -->

    <Spinner
        android:id="@+id/fragment_filter_room_spinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_normal"
        app:layout_constraintTop_toBottomOf="@id/fragment_filter_room_tv"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:textAlignment="center"
        android:background="@drawable/spinner"
        android:backgroundTint="@color/colorAccent"
        android:padding="@dimen/padding_little"/>

    <!-- BUTTON -->

    <Button
        android:id="@+id/fragment_filter_room_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_normal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="@string/filter_by_room"
        style="@style/FilledButton" />

</android.support.constraint.ConstraintLayout>